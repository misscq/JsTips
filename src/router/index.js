//配置路由相关信息
import Vue from 'vue'
import Router from 'vue-router'

//通过Vue.use(插件),安装插件
Vue.use(Router);

//创建Router对象
//映射关系
const routes = [
    
]
const router = new Router([
    //配置路由和组件之间的应用关系
    routes
])

//将router传到vue实例中
export default router